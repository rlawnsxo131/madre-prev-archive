import { css } from '@emotion/react';

const listBlock = css`
  list-style: none;
  margin: 0;
  padding: 0;
  display: flex;
  flex-direction: column;
  justify-content: center;
`;

export default {
  listBlock,
};
