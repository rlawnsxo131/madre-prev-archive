import { css } from '@emotion/react';

interface NotFoundPageProps {}

function NotFoundPage(props: NotFoundPageProps) {
  return <div css={block}>not found</div>;
}

const block = css``;

export default NotFoundPage;
