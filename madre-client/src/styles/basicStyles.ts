import { css } from '@emotion/react';

const button = css`
  padding: 0;
  border: 0;
  box-sizing: border-box;
  line-height: normal;
  background: none;
  cursor: pointer;
`;

const basicStyles = {
  button,
};

export default basicStyles;
